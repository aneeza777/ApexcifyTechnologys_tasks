name: random_quote_generator
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Required Packages
  http: ^1.1.0                   # For API calls
  shared_preferences: ^2.2.2     # For saving favorites & settings
  share_plus: ^7.2.1             # For sharing quotes
  # Clipboard is built into Flutter -> No extra package needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
